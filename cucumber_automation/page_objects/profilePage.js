"use strict"

// Requires to be logged in

const assert = require ("assert")
const {By, until} = require("selenium-webdriver")
const {openBrowser} = require("./browser")

/*
assert.equal(this.actual, "ab-item")
*/